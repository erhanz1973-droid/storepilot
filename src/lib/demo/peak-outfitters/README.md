# Peak Outfitters — Legacy scenario base data

**Fictional data only.** Used as the parameterized base for `struggling`, `scaling`, and `seasonal` demo scenarios.

> **App Store / website showcase:** use **Alpine Outfitters** (`src/lib/demo/alpine-outfitters/`) via the `healthy_growth` scenario and `src/lib/demo/provider.ts`.

## Isolation

- Store ID: `00000000-0000-4000-8000-000000000001`
- `allowDemoData()` / `STOREPILOT_ALLOW_DEMO` gates all synthetic data
- Production: demo ALWAYS disabled (flag ignored)
